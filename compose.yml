services:
  bot:
    build: .
    restart: unless-stopped
    command: ["npx", "dotenvx", "run", "-f", ".env.dev", "--", "node", "src/index.js"]
    env_file:
      - .env.keys
    volumes:
      - ./.env.dev:/app/.env.dev:ro
      - bot-data:/app/data

volumes:
  bot-data:
